# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add binary light/dark mode with CSS var tokens, replacing all hardcoded Tailwind colors across 30+ components.

**Architecture:** CSS custom properties in `:root`/`.dark` define the palette. Tailwind config extends `colors` to reference the vars. A `ThemeProvider` client component manages the toggle and persists to localStorage. A blocking head script prevents FOUC. A new `toggle_theme` SDUI action lets the middleend place the toggle button anywhere.

**Tech Stack:** Tailwind CSS 3 (darkMode: "class"), React Context, localStorage, Next.js App Router.

**Spec:** `docs/superpowers/specs/2026-04-15-dark-mode-design.md`

---

### Task 1: Token System — CSS vars + Tailwind config

**Files:**
- Modify: `app/globals.css`
- Modify: `tailwind.config.ts`

- [ ] **Step 1: Add CSS custom properties to globals.css**

Replace the entire file:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --bg-primary: 255 255 255;
  --bg-secondary: 249 250 251;
  --bg-muted: 243 244 246;
  --text-primary: 17 24 39;
  --text-secondary: 75 85 99;
  --text-muted: 156 163 175;
  --text-on-accent: 255 255 255;
  --border-default: 229 231 235;
  --border-input: 209 213 219;
  --accent-primary: 37 99 235;
  --accent-primary-hover: 29 78 216;
  --status-error: 220 38 38;
  --status-error-surface: 254 226 226;
  --status-success: 22 163 74;
  --status-warning: 202 138 4;
  --overlay: 0 0 0;

  --chart-1: oklch(0.65 0.18 250);
  --chart-2: oklch(0.7 0.18 145);
  --chart-3: oklch(0.72 0.18 35);
  --chart-4: oklch(0.7 0.2 310);
  --chart-5: oklch(0.75 0.15 75);
}

.dark {
  --bg-primary: 17 24 39;
  --bg-secondary: 31 41 55;
  --bg-muted: 55 65 81;
  --text-primary: 249 250 251;
  --text-secondary: 156 163 175;
  --text-muted: 107 114 128;
  --text-on-accent: 255 255 255;
  --border-default: 55 65 81;
  --border-input: 75 85 99;
  --accent-primary: 96 165 250;
  --accent-primary-hover: 147 197 253;
  --status-error: 248 113 113;
  --status-error-surface: 69 26 26;
  --status-success: 74 222 128;
  --status-warning: 250 204 21;
  --overlay: 0 0 0;

  --chart-1: oklch(0.78 0.15 250);
  --chart-2: oklch(0.8 0.15 145);
  --chart-3: oklch(0.82 0.15 35);
  --chart-4: oklch(0.8 0.17 310);
  --chart-5: oklch(0.85 0.12 75);
}
```

- [ ] **Step 2: Update tailwind.config.ts**

```ts
import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "class",
  content: ["./app/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        surface: {
          primary: "rgb(var(--bg-primary) / <alpha-value>)",
          secondary: "rgb(var(--bg-secondary) / <alpha-value>)",
          muted: "rgb(var(--bg-muted) / <alpha-value>)",
        },
        content: {
          primary: "rgb(var(--text-primary) / <alpha-value>)",
          secondary: "rgb(var(--text-secondary) / <alpha-value>)",
          muted: "rgb(var(--text-muted) / <alpha-value>)",
          "on-accent": "rgb(var(--text-on-accent) / <alpha-value>)",
        },
        border: {
          DEFAULT: "rgb(var(--border-default) / <alpha-value>)",
          input: "rgb(var(--border-input) / <alpha-value>)",
        },
        accent: {
          primary: "rgb(var(--accent-primary) / <alpha-value>)",
          "primary-hover": "rgb(var(--accent-primary-hover) / <alpha-value>)",
        },
        status: {
          error: "rgb(var(--status-error) / <alpha-value>)",
          "error-surface":
            "rgb(var(--status-error-surface) / <alpha-value>)",
          success: "rgb(var(--status-success) / <alpha-value>)",
          warning: "rgb(var(--status-warning) / <alpha-value>)",
        },
        overlay: "rgb(var(--overlay) / <alpha-value>)",
      },
    },
  },
  plugins: [],
};

export default config;
```

- [ ] **Step 3: Verify build**

Run: `./cli build`
Expected: SUCCESS.

- [ ] **Step 4: Commit**

```bash
git add app/globals.css tailwind.config.ts
git commit -m "feat: add CSS token vars and Tailwind semantic color config for dark mode"
```

---

### Task 2: ThemeProvider + FOUC prevention + layout wiring

**Files:**
- Create: `components/theme-provider.tsx`
- Modify: `app/layout.tsx`
- Test: `tests/theme-provider.test.ts`

- [ ] **Step 1: Write the test**

```ts
// tests/theme-provider.test.ts
import { describe, it, expect, beforeEach } from "vitest";

describe("theme-provider", () => {
  beforeEach(() => {
    localStorage.clear();
    document.documentElement.classList.remove("dark");
  });

  it("defaults to light when localStorage is empty", async () => {
    const { renderHook } = await import("@testing-library/react");
    const { ThemeProvider, useTheme } = await import(
      "@/components/theme-provider"
    );
    const wrapper = ({ children }: { children: React.ReactNode }) => (
      <ThemeProvider>{children}</ThemeProvider>
    );
    const { result } = renderHook(() => useTheme(), { wrapper });
    expect(result.current.theme).toBe("light");
    expect(document.documentElement.classList.contains("dark")).toBe(false);
  });

  it("reads dark from localStorage and applies class", async () => {
    localStorage.setItem("theme", "dark");
    const { renderHook } = await import("@testing-library/react");
    const { ThemeProvider, useTheme } = await import(
      "@/components/theme-provider"
    );
    const wrapper = ({ children }: { children: React.ReactNode }) => (
      <ThemeProvider>{children}</ThemeProvider>
    );
    const { result } = renderHook(() => useTheme(), { wrapper });
    expect(result.current.theme).toBe("dark");
    expect(document.documentElement.classList.contains("dark")).toBe(true);
  });

  it("toggle flips theme and persists", async () => {
    const { renderHook, act } = await import("@testing-library/react");
    const { ThemeProvider, useTheme } = await import(
      "@/components/theme-provider"
    );
    const wrapper = ({ children }: { children: React.ReactNode }) => (
      <ThemeProvider>{children}</ThemeProvider>
    );
    const { result } = renderHook(() => useTheme(), { wrapper });
    expect(result.current.theme).toBe("light");
    act(() => result.current.toggle());
    expect(result.current.theme).toBe("dark");
    expect(localStorage.getItem("theme")).toBe("dark");
    expect(document.documentElement.classList.contains("dark")).toBe(true);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm vitest run tests/theme-provider.test.ts`
Expected: FAIL — module not found.

- [ ] **Step 3: Create ThemeProvider**

```tsx
// components/theme-provider.tsx
"use client";

import {
  createContext,
  useCallback,
  useContext,
  useEffect,
  useState,
  type ReactNode,
} from "react";

type Theme = "light" | "dark";

type ThemeContextValue = {
  theme: Theme;
  toggle: () => void;
};

const ThemeContext = createContext<ThemeContextValue>({
  theme: "light",
  toggle: () => {},
});

export function ThemeProvider({ children }: { children: ReactNode }) {
  const [theme, setTheme] = useState<Theme>(() => {
    if (typeof window === "undefined") return "light";
    return (localStorage.getItem("theme") as Theme) ?? "light";
  });

  useEffect(() => {
    const root = document.documentElement;
    if (theme === "dark") {
      root.classList.add("dark");
    } else {
      root.classList.remove("dark");
    }
    localStorage.setItem("theme", theme);
  }, [theme]);

  const toggle = useCallback(() => {
    setTheme((prev) => (prev === "light" ? "dark" : "light"));
  }, []);

  return (
    <ThemeContext.Provider value={{ theme, toggle }}>
      {children}
    </ThemeContext.Provider>
  );
}

export function useTheme() {
  return useContext(ThemeContext);
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pnpm vitest run tests/theme-provider.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Wire into layout with FOUC prevention**

Modify `app/layout.tsx` to add `ThemeProvider` wrapping the body and a blocking script in head. The FOUC script is a hardcoded constant string (reads localStorage, adds class) — it contains no external input. Move `suppressHydrationWarning` to `<html>` because the script may add `dark` class before React hydrates.

- [ ] **Step 6: Verify build**

Run: `./cli build`
Expected: SUCCESS.

- [ ] **Step 7: Commit**

```bash
git add components/theme-provider.tsx app/layout.tsx tests/theme-provider.test.ts
git commit -m "feat: add ThemeProvider with localStorage persistence and FOUC prevention"
```

---

### Task 3: Migrate Text.tsx color map

**Files:**
- Modify: `components/base/Text.tsx`

- [ ] **Step 1: Update colorStyles map**

Replace the `colorStyles` object:

```ts
const colorStyles: Record<string, string> = {
  primary: "text-content-primary",
  secondary: "text-content-secondary",
  muted: "text-content-muted",
  error: "text-status-error",
  positive: "text-status-success",
  negative: "text-status-error",
};
```

- [ ] **Step 2: Verify build**

Run: `./cli build`
Expected: SUCCESS.

- [ ] **Step 3: Commit**

```bash
git add components/base/Text.tsx
git commit -m "feat: migrate Text.tsx to semantic color tokens"
```

---

### Task 4: Migrate Button.tsx + add toggle_theme action

**Files:**
- Modify: `components/base/Button.tsx`

- [ ] **Step 1: Update variant styles and add toggle_theme**

Replace `variantStyles`:

```ts
const variantStyles: Record<string, Record<string, string>> = {
  primary: {
    solid: "bg-accent-primary text-content-on-accent",
    ghost: "text-accent-primary",
    outline: "border border-accent-primary text-accent-primary",
  },
  secondary: {
    solid: "bg-surface-muted text-content-primary",
    ghost: "text-content-secondary",
    outline: "border border-border-input text-content-secondary",
  },
};
```

Add import: `import { useTheme } from "@/components/theme-provider";`

Add in component body: `const { toggle } = useTheme();`

Add case in `handleClick`:

```ts
case "toggle_theme":
  toggle();
  break;
```

- [ ] **Step 2: Verify build**

Run: `./cli build`
Expected: SUCCESS.

- [ ] **Step 3: Commit**

```bash
git add components/base/Button.tsx
git commit -m "feat: migrate Button to tokens and add toggle_theme action"
```

---

### Task 5: Migrate form inputs (Input, Select, Textarea, RadioGroup, Checkbox)

**Files:**
- Modify: `components/base/Input.tsx`
- Modify: `components/base/Select.tsx`
- Modify: `components/base/Textarea.tsx`
- Modify: `components/base/RadioGroup.tsx`
- Modify: `components/base/Checkbox.tsx`

Migration mapping for all five:

| Hardcoded | Token |
|---|---|
| `text-gray-700` | `text-content-secondary` |
| `text-red-500` | `text-status-error` |
| `text-gray-400` | `text-content-muted` |
| `bg-gray-100` | `bg-surface-muted` |
| `border-gray-300` | `border-border-input` |
| `border` (bare, on inputs) | `border border-border-input` |

- [ ] **Step 1: Migrate Input.tsx**

- Label class: `"block text-sm font-medium text-content-secondary mb-1"`
- Required span: `"text-status-error ml-0.5"`
- Disabled class: `" opacity-50 cursor-not-allowed bg-surface-muted"`
- Input class: `` `border border-border-input rounded px-3 py-2 w-full${disabledClass}` ``

- [ ] **Step 2: Migrate Select.tsx**

Same mapping as Input. Label, required, disabled, select border.

- [ ] **Step 3: Migrate Textarea.tsx**

Same mapping as Input. Label, required, disabled, textarea border.

- [ ] **Step 4: Migrate RadioGroup.tsx**

- Legend: `text-content-secondary`
- Required: `text-status-error`
- Disabled label: `text-content-muted`

- [ ] **Step 5: Migrate Checkbox.tsx**

- Checkbox border: `border-border-input` (replace `border-gray-300`)
- Disabled label: `text-content-muted`

- [ ] **Step 6: Verify build**

Run: `./cli build`
Expected: SUCCESS.

- [ ] **Step 7: Commit**

```bash
git add components/base/Input.tsx components/base/Select.tsx components/base/Textarea.tsx components/base/RadioGroup.tsx components/base/Checkbox.tsx
git commit -m "feat: migrate form input components to semantic color tokens"
```

---

### Task 6: Migrate Toggle + Loading

**Files:**
- Modify: `components/base/Toggle.tsx`
- Modify: `components/base/Loading.tsx`

- [ ] **Step 1: Migrate Toggle.tsx**

| Hardcoded | Token |
|---|---|
| `bg-blue-600` (on) | `bg-accent-primary` |
| `bg-gray-300` (off) | `bg-surface-muted` |
| `bg-white` (thumb) | `bg-surface-primary` |
| `text-gray-400` (disabled) | `text-content-muted` |

- [ ] **Step 2: Migrate Loading.tsx**

| Hardcoded | Token |
|---|---|
| `bg-gray-200` (skeleton) | `bg-surface-muted` |
| `text-gray-400` (spinner text) | `text-content-muted` |

- [ ] **Step 3: Verify build**

Run: `./cli build`
Expected: SUCCESS.

- [ ] **Step 4: Commit**

```bash
git add components/base/Toggle.tsx components/base/Loading.tsx
git commit -m "feat: migrate Toggle and Loading to semantic color tokens"
```

---

### Task 7: Migrate Badge, Snackbar, Error

**Files:**
- Modify: `components/base/Badge.tsx`
- Modify: `components/base/Snackbar.tsx`
- Modify: `components/base/Error.tsx`

- [ ] **Step 1: Migrate Badge.tsx**

Replace `variantStyles`:

```ts
const variantStyles: Record<string, string> = {
  error: "bg-status-error",
  info: "bg-accent-primary",
  warning: "bg-status-warning",
  success: "bg-status-success",
};
```

Badge text: `text-white` -> `text-content-on-accent`.

- [ ] **Step 2: Migrate Snackbar.tsx**

Replace `variantStyles`:

```ts
const variantStyles: Record<string, string> = {
  success: "bg-status-success",
  error: "bg-status-error",
  info: "bg-accent-primary",
  warning: "bg-status-warning",
};
```

Text: `text-white` -> `text-content-on-accent`.

- [ ] **Step 3: Migrate Error.tsx**

| Hardcoded | Token |
|---|---|
| `text-red-600` (message) | `text-status-error` |
| `border border-red-200` (container) | `border border-status-error-surface` |
| `text-red-700` (retry) | `text-status-error` |
| `hover:text-red-900` (retry hover) | remove, single token suffices |

Add `bg-status-error-surface` as background to the error container.

- [ ] **Step 4: Verify build**

Run: `./cli build`
Expected: SUCCESS.

- [ ] **Step 5: Commit**

```bash
git add components/base/Badge.tsx components/base/Snackbar.tsx components/base/Error.tsx
git commit -m "feat: migrate Badge, Snackbar, Error to semantic color tokens"
```

---

### Task 8: Migrate Modal + Screen

**Files:**
- Modify: `components/base/Modal.tsx`
- Modify: `components/base/Screen.tsx`

- [ ] **Step 1: Migrate Modal.tsx**

| Hardcoded | Token |
|---|---|
| `bg-black bg-opacity-50` (backdrop) | `bg-overlay/50` |
| `bg-white` (panel) | `bg-surface-primary` |
| `border-b` (title border) | `border-b border-border` |

- [ ] **Step 2: Migrate Screen.tsx**

| Hardcoded | Token |
|---|---|
| `text-gray-600` (back button) | `text-content-secondary` |
| `hover:text-gray-900` (back hover) | `hover:text-content-primary` |
| `text-gray-500` (subtitle) | `text-content-muted` |

- [ ] **Step 3: Verify build**

Run: `./cli build`
Expected: SUCCESS.

- [ ] **Step 4: Commit**

```bash
git add components/base/Modal.tsx components/base/Screen.tsx
git commit -m "feat: migrate Modal and Screen to semantic color tokens"
```

---

### Task 9: Migrate nav shell (NavItem, NavHeader, NavFooter, BottomBar)

**Files:**
- Modify: `components/base/NavItem.tsx`
- Modify: `components/base/NavHeader.tsx`
- Modify: `components/base/NavFooter.tsx`
- Modify: `components/base/BottomBar.tsx`

NavMain.tsx has no color classes — skip.

- [ ] **Step 1: Migrate NavItem.tsx**

| Hardcoded | Token |
|---|---|
| `hover:bg-gray-100` | `hover:bg-surface-secondary` |
| `text-gray-500` (icon) | `text-content-muted` |
| `bg-red-500` (badge) | `bg-status-error` |
| `text-white` (badge text) | `text-content-on-accent` |

- [ ] **Step 2: Migrate NavHeader.tsx**

Border: `border-b` -> `border-b border-border`.

- [ ] **Step 3: Migrate NavFooter.tsx**

Border: `border-t` -> `border-t border-border`.

- [ ] **Step 4: Migrate BottomBar.tsx**

| Hardcoded | Token |
|---|---|
| `bg-white` | `bg-surface-primary` |
| `border-t` | `border-t border-border` |

- [ ] **Step 5: Verify build**

Run: `./cli build`
Expected: SUCCESS.

- [ ] **Step 6: Commit**

```bash
git add components/base/NavItem.tsx components/base/NavHeader.tsx components/base/NavFooter.tsx components/base/BottomBar.tsx
git commit -m "feat: migrate nav shell components to semantic color tokens"
```

---

### Task 10: Migrate content (Card, ListItem, Divider, Table, TableRow)

**Files:**
- Modify: `components/base/Card.tsx`
- Modify: `components/base/ListItem.tsx`
- Modify: `components/base/Divider.tsx`
- Modify: `components/base/Table.tsx`
- Modify: `components/base/TableRow.tsx`

- [ ] **Step 1: Migrate Card.tsx**

Border: `border` -> `border border-border`.

- [ ] **Step 2: Migrate ListItem.tsx**

| Hardcoded | Token |
|---|---|
| `border-b` | `border-b border-border` |
| `hover:bg-gray-50` | `hover:bg-surface-secondary` |

- [ ] **Step 3: Migrate Divider.tsx**

`border-l` -> `border-l border-border`, `border-t` (or hr default) -> add `border-border` color.

- [ ] **Step 4: Migrate Table.tsx**

| Hardcoded | Token |
|---|---|
| `border-b` (header) | `border-b border-border` |
| `bg-gray-50` (header bg) | `bg-surface-secondary` |
| `text-gray-600` (header text) | `text-content-secondary` |

- [ ] **Step 5: Migrate TableRow.tsx**

| Hardcoded | Token |
|---|---|
| `border-b` | `border-b border-border` |
| `hover:bg-gray-50` | `hover:bg-surface-secondary` |

- [ ] **Step 6: Verify build**

Run: `./cli build`
Expected: SUCCESS.

- [ ] **Step 7: Commit**

```bash
git add components/base/Card.tsx components/base/ListItem.tsx components/base/Divider.tsx components/base/Table.tsx components/base/TableRow.tsx
git commit -m "feat: migrate Card, ListItem, Divider, Table, TableRow to semantic color tokens"
```

---

### Task 11: Migrate chart components (LineChart, PieChart)

**Files:**
- Modify: `components/custom/LineChart.tsx`
- Modify: `components/custom/PieChart.tsx`

- [ ] **Step 1: Migrate LineChart.tsx tooltip + empty state**

| Hardcoded | Token |
|---|---|
| `bg-white` (tooltip) | `bg-surface-primary` |
| `border` (tooltip) | `border border-border` |
| `text-gray-900` (tooltip label/value) | `text-content-primary` |
| `text-gray-600` (tooltip series) | `text-content-secondary` |
| `text-gray-500` (empty state) | `text-content-muted` |

- [ ] **Step 2: Migrate PieChart.tsx tooltip + legend**

| Hardcoded | Token |
|---|---|
| `bg-white` (tooltip) | `bg-surface-primary` |
| `border` (tooltip) | `border border-border` |
| `text-gray-900` (label) | `text-content-primary` |
| `text-gray-700` (value) | `text-content-secondary` |
| `text-gray-500` (pct + empty) | `text-content-muted` |

- [ ] **Step 3: Verify build**

Run: `./cli build`
Expected: SUCCESS.

- [ ] **Step 4: Commit**

```bash
git add components/custom/LineChart.tsx components/custom/PieChart.tsx
git commit -m "feat: migrate chart components to semantic color tokens"
```

---

### Task 12: Update specs + SCAFFOLDING_GAPS.md

**Files:**
- Modify: `spec/sdui-actions.md`
- Modify: `SCAFFOLDING_GAPS.md`

- [ ] **Step 1: Add toggle_theme to sdui-actions.md section 2 table**

Add row:

```
| `toggle_theme`  | Toggles light/dark mode. Client-side only, no round-trip. | (none) |
```

- [ ] **Step 2: Add gap section 10 to SCAFFOLDING_GAPS.md**

Document: scaffold ships no dark mode infrastructure; components use hardcoded Tailwind colors; no ThemeProvider; no toggle action type. Reference the design spec. List the 15 CSS var tokens, the ThemeProvider, the FOUC script, and the full component migration (30+ files) as what every scaffold project would need to add.

- [ ] **Step 3: Commit**

```bash
git add spec/sdui-actions.md SCAFFOLDING_GAPS.md
git commit -m "docs: add toggle_theme to action spec and dark mode to scaffold gaps"
```

---

### Task 13: Full verification

- [ ] **Step 1: Run full verification**

Run: `rm -rf .next && ./cli build && ./cli lint && ./cli test`
Expected: All three pass.

- [ ] **Step 2: Run scaffold verify**

Run: `/Users/vadimkent/.claude/plugins/data/workflow-vk-workflow/scaffold verify /Users/vadimkent/repos/vk_investment_frontend_v2`
Expected: All checks SUCCESS.

- [ ] **Step 3: Grep for remaining hardcoded color classes**

Run: `grep -rn "bg-white\|bg-gray-\|bg-black\|bg-blue-\|bg-red-\|bg-green-\|bg-yellow-\|text-white\|text-gray-\|text-red-\|text-green-\|text-blue-\|text-yellow-\|border-gray-\|border-red-\|border-blue-" components/ --include="*.tsx"`

Expected: Zero matches. Any remaining means a component was missed.

- [ ] **Step 4: Fix any issues found and format**

```bash
pnpm prettier --write .
git add -A
git commit -m "feat: complete dark mode migration — all components use semantic tokens"
```
